import AddButton from "../Buttons/AddButton";

const DocumentsHeader: React.FC = () => {

    return (
        <header>
            <AddButton></AddButton>
        </header>
    )
}

export default DocumentsHeader;